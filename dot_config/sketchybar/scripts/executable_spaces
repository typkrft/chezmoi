#/usr/bin/env bash
# set background of active space
if [ "$SELECTED" = "true" ]; then
  sketchybar -m --set $NAME icon.highlight=on background.drawing=on
else
  sketchybar -m --set $NAME icon.highlight=off background.drawing=off
fi